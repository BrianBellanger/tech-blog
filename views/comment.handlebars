^^^^^comment.hand^^^^^^<section class="comment">
  <div class="comment-details">
    <h3>By-  **user.username**{{user.username}}: **comment.body**{{comment.body}}</h3>
  </div>
</section>^^^^^comment.hand^^^^^^