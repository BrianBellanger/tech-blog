^^^^^ post.hand^^^^^^<section class="container">
  {{log post}}
  <h2 class="post-name"> Post By-{{post.user.username}}: {{post.name}}  [{{post.createdAt}}]</h2>
  <h4>{{post.description}}</h4>
  <section class="flex-row justify-left">
    {{#each post.comments as |comment|}}
    {{> comment-details}}
    {{/each}}
    <p>Add Comment</p>
  </section>
</section>^^^^^ post.hand^^^^^^